package com.afternote.core.model

/**
 * 마음의 기록 카테고리.
 *
 * UI 리소스(아이콘·표시 문자열)는 피처 모듈에서 확장 프로퍼티로 제공한다.
 */
enum class MindRecordCategory {
    DAILY_QUESTION,
    DIARY,
    DEEP_THOUGHT,
    WEEKLY_REPORT,
}
