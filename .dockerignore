# docker build 컨텍스트 축소용. Dockerfile.screenshot 는 COPY/ADD 가 없어
# 빌드 컨텍스트를 실제로 쓰지 않는다(프로젝트는 run 시 -v 바인드마운트). 전송량만 줄인다.
**/build/
build/
.gradle/
**/.gradle/
**/.cxx/
.git/
.idea/
*.iml
gha-creds-*.json
