<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
          디버그 빌드에서는 크래시·오류 수집을 끕니다.
          non-fatal 계측(#536) 이후 로그인 실패가 전부 기록되는데, 개발 중 실패(백엔드 미기동,
          미등록 키 해시 등)까지 배포본과 같은 콘솔에 섞이면 실제 장애 신호를 덮습니다.
          검증은 release 빌드로 합니다.
        -->
        <meta-data
                android:name="firebase_crashlytics_collection_enabled"
                android:value="false" />
        <activity
                android:name="com.afternote.afternote_fe.debug.DebugSettingsActivity"
                android:exported="true"
                android:label="Afternote DEV"
                android:taskAffinity=".devsettings"
                android:excludeFromRecents="true"
                android:theme="@style/Theme.Afternotefe.Main" />
        <!--
          계측 테스트 전용 빈 Activity. setContent 와 hiltViewModel() 을 함께 쓰기 위한
          자리라 exported 하지 않고 런처에도 올리지 않는다.
        -->
        <activity
                android:name="com.afternote.afternote_fe.test.HiltTestActivity"
                android:exported="false"
                android:theme="@style/Theme.Afternotefe.Main" />
        <!--
          LAUNCHER intent-filter를 의도적으로 두지 않습니다.
          런처 아이콘 중복(.MainActivity와 2개) 방지 — 디버그 설정 화면은
          `adb shell am start -n com.afternote.afternote_fe/.debug.DebugSettingsActivity` 로 진입합니다. (#396)
        -->
    </application>

</manifest>
