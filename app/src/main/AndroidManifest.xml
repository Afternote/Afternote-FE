<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      광고 SDK 미사용 — Analytics 는 Crashlytics breadcrumb 목적뿐이므로,
      firebase-analytics(play-services-measurement)가 병합해 오는 광고 ID 권한을 제거한다.
      https://developer.android.com/build/manage-manifests#node_markers
    -->
    <uses-permission
            android:name="com.google.android.gms.permission.AD_ID"
            tools:node="remove" />
    <uses-permission
            android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
            tools:node="remove" />

    <application
            android:name=".GlobalApplication"
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Afternotefe.Main"
            android:usesCleartextTraffic="false">
        <!--
          광고 ID 수집·광고 개인화 신호 비활성화 — Analytics 는 breadcrumb 전용.
          https://firebase.google.com/docs/analytics/android/configure-data-collection
        -->
        <meta-data
                android:name="google_analytics_adid_collection_enabled"
                android:value="false" />
        <meta-data
                android:name="google_analytics_default_allow_ad_personalization_signals"
                android:value="false" />
        <meta-data
                android:name="firebase_messaging_installation_id_enabled"
                android:value="true" />
        <meta-data
                android:name="com.google.firebase.messaging.default_notification_icon"
                android:resource="@drawable/core_common_logo" />
        <meta-data
                android:name="com.google.firebase.messaging.default_notification_channel_id"
                android:value="@string/fcm_notification_channel_id" />

        <!--
          App Startup — 기동 초기화를 Initializer 로 나눠 실행한다.
          androidx.startup 이 제공하는 provider 에 병합하는 형태라 tools:node="merge" 가 필요하다.
        -->
        <provider
                android:name="androidx.startup.InitializationProvider"
                android:authorities="${applicationId}.androidx-startup"
                android:exported="false"
                tools:node="merge">
            <meta-data
                    android:name="com.afternote.afternote_fe.startup.DailyNotificationInitializer"
                    android:value="androidx.startup" />
            <meta-data
                    android:name="com.afternote.afternote_fe.startup.KakaoInitializer"
                    android:value="androidx.startup" />
        </provider>

        <service
                android:name=".messaging.AfternoteFirebaseMessagingService"
                android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
          현재 화면은 폰 세로 레이아웃만 지원한다. Android 16 대화면에서는 시스템이
          이 제한을 무시하므로 대화면 대응을 대신하는 선언은 아니다 (#1144).
        -->
        <!--
          windowSoftInputMode="adjustResize" — 키보드 inset 을 Compose 로 흘려보내는 선언이다.
          enableEdgeToEdge() 를 쓰면 키보드가 떠도 윈도우 프레임이 줄지 않고, 그 대신 IME inset 이
          와야 하는데 이 선언이 없으면 그마저 오지 않아 `Modifier.imePadding()` 이 어디에 붙어 있든
          0 을 더한다 — 하단 CTA 가 키보드에 덮였다 (#1849).
        -->
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:icon="@mipmap/ic_launcher"
                android:roundIcon="@mipmap/ic_launcher_round"
                android:screenOrientation="portrait"
                android:theme="@style/Theme.Afternotefe.Starting"
                android:windowSoftInputMode="adjustResize"
                tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            tools:ignore="Instantiatable" — lint 오탐이다. Kakao SDK 2.24.0 부터 이 클래스가
            android.app.Activity 를 상속하지 않는다고 보고하지만, aar 안의 계층은 그대로다
            (AuthCodeHandlerActivity -> CustomTabLauncherActivity -> androidx.appcompat.app.AppCompatActivity,
            바이트코드 버전·클래스 수까지 2.23.2 와 동일). 2.23.2 와 다른 점은 2.24.0 이 Gradle Module
            Metadata 를 함께 배포해 aar variant 만 노출한다는 것뿐이고, appcompat 은 릴리스 런타임
            클래스패스에 정상 리졸브된다(1.7.1).

            R8 축소한 릴리스 APK 에 kakao<앱키>://oauth 딥링크를 던져 실제로 인스턴스화되는 것을
            확인했다 — 시스템이 프로세스를 띄우고 이 액티비티를 최상단으로 올렸으며 인스턴스화 실패·
            ClassNotFoundException 은 나오지 않았다 (#1188).

            이 억제는 이 컴포넌트 한 곳에만 건다. lint 규칙 자체를 끄면 진짜 Instantiatable 회귀를 놓친다.
        -->
        <activity
                android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
                android:exported="true"
                tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                        android:host="oauth"
                        android:scheme="kakao${KAKAO_NATIVE_APP_KEY}" />
            </intent-filter>
        </activity>
    </application>

</manifest>
