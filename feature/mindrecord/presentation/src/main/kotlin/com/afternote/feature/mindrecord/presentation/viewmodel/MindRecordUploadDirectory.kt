package com.afternote.feature.mindrecord.presentation.viewmodel

/**
 * `POST /files/presigned-url` 의 `directory` 값. 서버 허용 목록
 * (profiles / timeletters / afternotes / mindrecords / documents) 중 마음의 기록 전용 폴더.
 */
internal const val MIND_RECORD_UPLOAD_DIRECTORY = "mindrecords"
