# 추측으로 진행한 항목

확정 근거(시안·명세·이슈 답변)가 없어 **판단해서 정한** 것들을 모아 둔다. 각 항목은 되돌리기
쉬운 형태로 구현했고, 확정 답변이 오면 여기 적힌 자리만 고치면 된다.

근거가 있는 결정은 여기 적지 않는다 — 이 문서에 있는 것은 전부 "나중에 뒤집힐 수 있다".

| 이슈 | PR | 추측한 것 | 확정되면 고칠 자리 |
|---|---|---|---|
| #627 | #894 | 홈 TODAY'S QUESTION `데일리질문 답변하기` 의 **목적지** | `AppNavigationActions.onAnswerClick()` 한 줄 |
| #442 | #896 | 임시저장 **삭제 실패 안내의 표현** | `DraftListScreen.DraftDeleteFailedToast` · `mindrecord_draft_list_delete_failed_toast` |
| #442 | #896 | 비활성 "선택 삭제" 라벨 **색** (`gray6`) | `DraftListScreen.DeleteActionBar` |
| #716 | #904 | 실패·진행 **표현** (기존 텍스트 재사용, 새 컴포넌트 미신설) | 각 화면의 진행/실패 `Text` — 표현 통일은 #446 |
| #592 | #909 | `getToday()` **실패 시 표시 방식** (영역을 그리지 않음) | `DailyQuestionAnswerListScreen.TodayRecommendedQuestion` |
| #751 | #908 | 날짜 파싱 실패 항목을 **목록에서 제외** (내용까지 숨김) | `MindRecordUiMapper.toUi()` · `WeeklyReportViewModel.toUi()` |
| #506 | #1052 | 홈 '수신인 지정 미완료' 칩의 **목적지** (등록 화면) | `AppNavigationActions.onRecipientChipClick()` 한 줄 |
| #611 | #1171 | 수신자 목록 전달 조건 미충족 **안내 문구** | `receiver_afternote_list_not_deliverable_message`·`_description` |
| #619 | #1169 | 계정 정보 부재의 **표현·문구** («남기지 않음») | `receiver_detail_account_absent`·`receiver_detail_account_value_absent` |
| #739 | #1216 | 인증번호 발송 중 버튼 **문구** («전송 중…») | `receiver_verify_code_requesting` |
| #554 | 64cf93888 | 수신인 검색 placeholder **문구** («이름으로 검색하기») | `setting_receiver_search_placeholder`·`timeletter_recipient_search_placeholder` |
| #1067 | #1449 | 본문 링크의 **스킴 없는 입력 처리**(https 로 읽음)와 **거절 안내 문구** | `toBodyLinkHrefOrNull` 의 `DEFAULT_SCHEME` · `mindrecord_link_sheet_invalid` |
| #648 | #1346 | 복수 선택 행의 **선택 컨트롤 비주얼** (라디오 시안 → core 원형 체크박스) | `PlaylistSongItem.SongSelectionCheckbox` |
| #490 | #1350 | 서비스 검색 **일치 규칙**과 카탈로그 밖 **기존 custom 값 처리** | `filterEditorServiceOptions` · `EditorFormState.currentServiceOptions` |

---

## #627 — TODAY'S QUESTION CTA 목적지

**정한 것**: `MindRecordRoute.DailyQuestionWriteRoute`(데일리질문 답변 작성 화면).

**근거가 없는 이유**: 이슈 본문이 "이 CTA 의 목적지(데일리질문 답변 작성 화면인지 기록 탭인지)가
시안에서 어떻게 정의돼 있는지는 확인하지 않았다" 로 남겨 뒀다. Figma 카드 노드에 프로토타입
연결이 없다.

**판단 근거**: 카드 문구가 "데일리질문 **답변하기**" 이고, 같은 동작의 기존 진입점(기록 탭 →
FAB)이 이 작성 화면이다. 기록 탭으로 보내면 사용자가 한 번 더 눌러야 한다.

**뒤집을 때**: `AppNavigationActions.onAnswerClick()` 의 `navigate(...)` 대상만 바꾼다.

## #442 — 삭제 실패 안내 표현

**정한 것**: 완료 토스트(Figma `2372:25211`)와 같은 자리·같은 모양에 문구만 다르게.
`"%1$d개를 삭제하지 못했어요. 다시 시도해 주세요."`

**근거가 없는 이유**: 시안에 삭제 **실패** 상태가 없다. 완료 토스트만 정의돼 있다.

**판단 근거**: 새 표현을 만들면 미확정 디자인이 제품 동작으로 굳는다. 이미 있는 컴포넌트를
재사용하고 문구만 바꾸는 쪽이 되돌리기 쉽다.

**뒤집을 때**: `DraftDeleteFailedToast` 컴포저블과 문자열 리소스만 교체한다. 상태 모델
(`DraftDeleteOutcome.SomeFailed`)은 표현과 무관하게 유지된다.

비활성 "선택 삭제" 라벨 색(`gray6`)도 같은 이유로 임시값이다.

## #716 — 실패·진행 표현

**정한 것**: 새 컴포넌트를 만들지 않고 기존 텍스트 표시에 문구와 재시도만 붙였다.

**근거가 없는 이유**: 실패 안내 컴포넌트의 표현 통일은 이슈 노트대로 #446 범위다.

**판단 근거**: 지금 새 표현을 만들면 #446 이 확정될 때 두 벌이 된다. 동작(재시도 결선·
문구 치환)만 고치고 표현은 그대로 뒀다.

## #592 — 오늘의 추천 질문 조회 실패

**정한 것**: `getToday()` 가 실패하면 그 영역을 그리지 않는다.

**근거가 없는 이유**: 시안에 이 영역의 실패 상태가 없다.

**판단 근거**: 목록 자체는 정상인 상황이라 보조 영역 하나 때문에 화면을 오류로 바꾸는 것은
과하다. 다만 이슈가 "실패가 드러나지 않는다" 를 지적한 것은 그대로 남는다.

**뒤집을 때**: `TodayRecommendedQuestion` 하나만 고치면 된다.

## #751 — 날짜 파싱 실패 항목 처리

**정한 것**: 목록에서 제외한다(내용도 함께 안 보인다).

**근거가 없는 이유**: 이슈가 세 선택지(제외 / 날짜 자리 비움 / 상위로 전파)를 제시하고
고르지 않았다.

**판단 근거**: 같은 파일의 `Diary.toUi()` 가 이미 "제외" 규칙을 쓰고 KDoc 에 근거가 있다.
새 정책을 만들기보다 갈라진 쪽을 맞추는 편이 낫다고 봤다.

**뒤집을 때**: 내용을 살리려면 `DailyQuestion.date` 를 nullable 로 바꾸고 카드가 날짜 자리를
비우게 한다.

## #789 — 기분 enum 의 "모르는 값" 처리

**정한 것**: 기분 필드의 **키 누락**은 파싱 실패로 두되, 클라가 모르는 **값**은
그 칸만 비운다. nullable 기분 필드(`weeklyDominantMood`, `week[].emotion`)에
`NullableTodayMoodSerializer` 를 달아 모르는 값을 `null` 로 접는다.

**근거가 없는 이유**: 이슈는 "기본값을 없애 계약 누락을 드러내라" 까지만 정했다.
그런데 `coerceInputValues` 는 생성자 기본값이 있어야 동작해서, 기본값을 걷어내면
키 누락뿐 아니라 값 확장까지 함께 실패로 바뀐다. 이슈가 이 부수 효과를 다루지 않았다.

**판단 근거**: 두 상황은 성격이 다르다. 키 누락은 계약이 바뀐 것이라 드러나야 하지만,
값 확장(서버가 기분 종류를 늘리거나 표기를 바꾸는 것)은 이모지 한 칸 대신 그 주/그 달
목록 전체를 날릴 일이 아니다. 감정 값이 한글로 관측된 전례도 있다 (#591).

**남는 위험**: 비-nullable 인 일기 항목의 `todayMood` 는 이슈 지시대로 non-null 이라,
모르는 값이 오면 **그 달 목록 전체**가 실패한다. 사용자가 3종 중 고르는 값이고 실서버가
`"SOSO"` 형태로 내려주는 것은 확인했지만, 서버가 종류를 늘리면 여기서 먼저 깨진다.

**뒤집을 때**: `DiaryListItemDto.todayMood` / `ReceiverDiaryItemDto.todayMood` 도
nullable 로 낮추고 같은 직렬화기를 달면 된다 (도메인 `Diary.todayMood` 까지 전파).

## #729 — 과거 주간리포트를 어디까지 제공할지

**정한 것**: 이번 주 포함 **52주(1년)**.

**근거가 없는 이유**: 이슈는 "고정 상수 5개가 아니라 제품에서 허용하는 과거 주간리포트
범위" 라고만 적었고, 그 범위가 얼마인지는 명세·시안 어디에도 없다. 시안의 열린 메뉴는
가시 항목 5개와 스크롤 표시만 보여줄 뿐 전체 개수를 알려주지 않는다.

**판단 근거**: 계정 생성 주까지로 좁히는 쪽이 가장 정확하지만 서버에 가입일이 없다
(`GET /users/me` 는 이름·이메일·전화·프로필만 준다). 기록이 없는 주를 골라도
`GET /mind-record` 는 0 건으로 정상 응답하므로 빈 주차를 보여주는 것 자체는 안전하고,
1년이 "지난 리포트를 돌아본다" 는 화면 목적에 무난한 상한이라고 봤다.

**뒤집을 때**: `WeeklyReportWeekOptions.kt` 의 `WEEK_OPTION_COUNT` 하나만 바꾸면 된다.
가입일 API 가 생기면 `buildWeekOptions(today, count)` 의 `count` 를 가입 주까지의 주 수로
계산해 넘기면 되고, 메뉴 높이(가시 항목 수)는 이 값과 무관하게 유지된다.

## #725 — 부분 실패를 어떤 상태로 볼지 / 폴링 간격

**정한 것 1**: `pending == 0 && succeeded > 0 && failed > 0`(일부만 실패)은
**완료**로 본다 — 건진 키워드를 그대로 보여준다.

**근거가 없는 이유**: 이슈는 "분석 대기, 분석 실패, 분석 완료, 실제 분석 대상 없음"
네 상태만 요구하고, 부분 실패가 어느 쪽인지는 정하지 않았다.

**판단 근거**: 실패 화면으로 덮으면 이미 성공한 키워드까지 가린다. "아무것도 건지지
못한 경우"만 실패로 두는 편이 손실이 적다고 봤다.

**뒤집을 때**: `EmotionAnalysis.status` 의 `succeeded == 0 && failed > 0` 조건 하나다.
부분 실패를 따로 표시하려면 `PARTIALLY_FAILED` 를 추가하고 카드에 분기를 하나 더 둔다.

**정한 것 2**: 분석 완료를 기다리는 재조회는 **8초 × 8회(약 1분)** 로 끊는다.

**근거가 없는 이유**: 분석 완료를 알려주는 채널(푸시·웹소켓·완료 플래그)이 없고,
분석이 보통 얼마나 걸리는지도 명세에 없다. 실측(2026-08-23)에서는 일기 1건이
1분 넘게 `pending` 이었고 그 세션에서는 끝나지 않았다.

**판단 근거**: 무한 폴링은 하지 않는다는 것만 확실했다. 화면에 머무는 동안 한 번은
잡을 만한 길이로 1분을 뒀고, 그 뒤는 화면 이탈·복귀(`refreshOnReturn`)와 사용자의
재시도 버튼에 맡긴다.

**뒤집을 때**: `WeeklyReportViewModel` 의 `EMOTION_ANALYSIS_POLL_ATTEMPTS` /
`EMOTION_ANALYSIS_POLL_INTERVAL_MILLIS` 두 상수다. 서버가 완료 신호를 주게 되면
(Afternote-BE#117) `awaitEmotionAnalysis` 를 그 구독으로 갈아끼우면 된다.

**남는 것**: FE 의 "다시 시도" 는 **재조회**일 뿐 서버 측 재분석 트리거가 아니다.
재시도 소진(`failed`) 상태에서 다시 분석을 걸 방법은 아직 없다 — Afternote-BE#117 소관.

## #547 — 주간리포트 날짜 파싱 실패 처리

**정한 것**: 해석하지 못한 날짜의 항목은 **DTO→도메인 경계에서 제외**한다.
도메인 `WeeklyReportDailyQuestion.date` 는 non-null `LocalDate` 로 올린다.

**근거가 없는 이유**: 이슈가 "실패 처리 정책(제외 / nullable 로 노출 후 UI 에서 날짜
비우기 등)을 한 곳에서 정한다" 라고만 적고 어느 쪽인지 고르지 않았다.

**판단 근거**: 같은 모듈이 이미 "제외" 로 수렴하고 있다 — `Diary.toUi()`,
`DailyQuestion.toUi()`(#751) 가 날짜를 못 정하면 항목을 뺀다. 새 정책을 하나 더
만들기보다 갈라진 쪽을 맞추는 편이 낫다고 봤다. 종전의 `LocalDate.now()` 폴백은
파싱 못 한 기록을 **오늘 작성한 것처럼** 보여 이상을 감추므로 어느 쪽으로 가든 폐기 대상이다.

**남는 위험**: 날짜만 깨진 항목의 제목·본문까지 함께 사라진다. 실서버는 늘
`yyyy.MM.dd 요일` 로 내려주고 세 포맷을 모두 허용하므로 실제로 걸릴 일은 적지만,
서버가 포맷을 바꾸면 HISTORY 가 통째로 비는 방향으로 실패한다. 매퍼가 로그를 남긴다.

**뒤집을 때**: `WeeklyReportDailyQuestion.date` 를 `LocalDate?` 로 낮추고
`toDomainOrNull()` 을 `toDomain()` 으로 되돌린 뒤, 카드가 날짜 자리를 비우게 한다.
집계는 이미 `mapNotNull` 이라 그대로 두면 된다.

## #618 · #614 — 수신자 화면의 구성과 문구

**정한 것 1 (#618)**: 받은 기록 본문을 **별도 라우트가 아니라 바텀시트**로 연다.
구성은 날짜·기분 → 제목 → 본문 순.

**근거가 없는 이유**: 이슈가 시안 미대조다. 수신자 기록 상세 화면의 시안이 있는지
확인하지 못했다.

**판단 근거**: 이 화면에는 뒤로가기가 없어(#614) 새 화면을 쌓으면 시스템 백키 외에
나올 방법이 없다. 시트는 스와이프·바깥 탭으로 닫힌다. 같은 모듈의 `MemoryDetailOverlay`
가 이미 쓰는 방식이기도 하다.

**뒤집을 때**: `ReceiverRecordDetailSheet` 한 컴포넌트만 고치면 된다. 라우트로 옮기려면
`MindRecordRoute` 에 항목 ID 를 받는 라우트를 더하고 화면에서 시트 호출을 네비게이션으로
바꾼다.

**정한 것 2 (#614)**: 전달 조건 미충족(403 / code 2009) 문구를
«아직 열람할 수 있는 기록이 없어요. 작성자가 전달 시점을 정하면 이곳에서 볼 수 있어요.»
로 적었다.

**근거가 없는 이유**: 이슈가 «대체 문구를 확정할 때 디자이너 확인이 선행돼야 한다» 고
명시했다. 확인을 받지 못했다.

**판단 근거**: 원문("아직 전달 조건이 충족되지 않았습니다")은 사용자가 무엇을 기다리는지
알 수 없다. 수신자가 할 수 있는 일이 없는 상태이므로 **기다리는 상태임과 누가 정하는지**를
알리는 쪽으로 적었다.

**뒤집을 때**: `mindrecord_receiver_delivery_not_ready` 문자열 하나다.

**남는 것**: 두 이슈 모두 **실기기 종단 확인을 하지 못했다.** 수신자 기록 화면은
열람 승인(`delivery-verification` APPROVED)된 계정이 있어야 열리는데, 승인은 관리자만
할 수 있고 수신자·발신자 API 어디에도 승인 경로가 없다. 403 응답 자체는 재현했고
(수신자 생성 → `X-Auth-Code` 조회 → `403 {"code":2009}`), 화면 동작은 Robolectric
Compose 테스트로 고정했다.

## #207 — 깊은 생각 제거 후 홈 요약 그리드 레이아웃

**정한 것**: `WeeklySummaryGrid` 의 «최근 깊은생각» 카드를 없애고, 오른쪽에 남은
«이번 주 기록 수» 카드가 **왼쪽 큰 카드와 같은 높이**를 채우게 했다 (3장 → 2장).

**근거가 없는 이유**: 깊은 생각 제거는 기획 결정으로 전달받았지만, 카드가 빠진 뒤의
그리드 시안은 받지 못했다.

**판단 근거**: 오른쪽 컬럼은 «작은 카드 2장 + 간격» 이 왼쪽 큰 카드 높이와 같도록
짜여 있었다. 한 장만 남기고 아래를 비우면 그리드가 어긋나므로, 남은 한 장을 늘려
2열 구성을 유지하는 쪽을 택했다.

**뒤집을 때**: `WeeklySummaryGrid` 한 파일이다. 카드를 다른 것으로 채우기로 하면
오른쪽 컬럼을 다시 `smallSquareSize` 두 장으로 되돌리면 된다.

**함께 정리한 것**: 서버는 `deepThoughtAmount`·`week[].type=DEEP_THOUGHT` 를 계속
내려준다. **DTO 에 선언하지 않아 그대로 무시**하고(#207), 기록일수 집계에서도
제외한다(#590). 서버가 나중에 필드를 빼도 앱은 영향받지 않는다.

## #759 데일리 질문·일기 상세 화면 — 시안에 없는 값들

시안 4종에 나타나지 않아 판단으로 정한 것들.

| 항목 | 정한 것 | 근거 |
|---|---|---|
| 헤더 배경 이미지의 출처 | 본문 HTML 의 **첫 `img`** | 서버가 대표 이미지를 따로 주지 않는다. 목록·상세 어느 응답에도 썸네일 필드가 없다. |
| 이미지가 여러 장일 때 | 첫 장만 헤더로, 나머지는 본문 순서대로 | 시안에 다중 이미지 변형이 없다. |
| 빈 `src` 의 `img` | 이미지 없음으로 취급 | 통과시키면 "이미지 O" 변형으로 그려진 뒤 영원히 비는 헤더가 된다. |
| 수신인이 여러 명일 때 | 이름을 이어 붙인다 | 시안은 한 명("박채연")만 보여 준다. |
| 수신인이 없을 때 | 줄 자체를 그리지 않는다 | 시안에 미지정 변형이 없다. "수신인" 라벨만 남는 편보다 낫다고 봤다. |
| 상세 진입 시 데이터 | 목록 API 를 다시 호출해 대상 하나를 고른다 | 단건 조회 API 가 없다(`GET /api/v1/diary`·`/daily-questions` 만 있음). 작성 화면 프리필과 같은 방식. |
| 조회 실패 | 상세 화면에 오류 문구 | 시안에 오류 변형이 없다. |

**이슈 본문과 실측이 다른 부분** — Note 의 "'프로필 설정하러가기' 요소가 포함돼 있어" 는
상세 4종(3814:18721·18813 확인)에서 찾을 수 없다. 하단 빈 44dp 는 nav 컴포넌트의 빈
슬롯이다. 구현하지 않았고, PR #1006 에 남겼다.

## #955 일기 수정 요청의 수신자 — "빈 선택" 의 뜻

서버는 `receiverIds` 를 세 가지로 읽는다(실측): 목록이 오면 그대로 반영, **생략하면 기존
유지**, **빈 목록이면 전체 해제**.

문제는 수정 화면 프리필이 기존 수신자를 불러오지 않는다는 것이다. 그래서 화면의 "고른 게
없음" 이 "원래 없었음" 인지 "사용자가 다 뺐음" 인지 구분되지 않는다.

**빈 선택은 생략(null)으로 보내기로 했다.** 빈 목록으로 보내면 수신자를 건드리지 않은
편집이 기존 지정을 지우고, 목록 응답에 수신자가 없어 되살릴 수도 없다. 반대로 생략을
고르면 잃는 것은 "전체 해제" 기능뿐이다 — 애초에 프리필이 없어 사용자가 표현할 수도 없던
동작이다. 생성 경로(#933)와도 같은 규칙이 된다.

프리필까지 붙으면(#759 가 목록 응답의 `receivers` 를 도메인에 싣는다) 빈 선택을 해제로
해석하는 쪽으로 바꿀 수 있다.

**분리한 것** — 일기의 기록 날짜는 생성·수정 어느 쪽으로도 서버에 보낼 수 없다(양쪽
스키마에 `date` 없음, 보내도 무시됨을 실측). 날짜 선택이 표시 전용인 문제는 BE 계약
확장이나 기획 결정이 필요해 #1008 로 분리했다.

## #1008 — 일기 기록 날짜 (해소, 추측 없음)

**서버가 `date` 를 받는다.** 2026-08-25 실측 당시에는 아니었고, 그래서 #1121 이 날짜 행을
표시 전용으로 잠갔다. 그 뒤 BE 가 (a) 계약 확장을 했다.

| 시점 | 상태 |
|---|---|
| 2026-08-25 (FE 실측) | `POST`·`PATCH` 에 `date: "2026-08-01"` 을 실어도 저장분은 `2026-08-25`. Swagger 요청 스키마에 `date` 없음 |
| 2026-08-29 (`Afternote-BE#244` → PR #262 머지) | `DiaryCreateRequest.date`·`DiaryUpdateRequest.date` 신설. `Diary.entry_date` 컬럼과 `(user_id, entry_date)` 인덱스, 월 조회·주간 리포트가 `entry_date` 기준 |

계약 규칙도 서버 코드에서 그대로 읽었다.

- 생성: 미전송이면 오늘(Asia/Seoul)
- 수정: **생략이면 기존 값 유지**
- 공통: 미래 날짜는 `400`(code `2101`), 과거는 제한 없음

FE 는 그 계약대로 되돌렸다 — 날짜 행에 피커를 다시 붙이고, 고른 값을 요청에 싣는다.
수정 경로는 «화면 값이 서버에서 왔거나 사용자가 고른 것일 때만» 싣는다. 프리필이 실패한 채
오늘을 실어 보내면 기존 기록일이 조용히 오늘로 옮겨지기 때문이다 — 수신자 목록을 빈 목록으로
보내면 안 되는 것과 같은 함정이다. `DiaryRecordDateWireTest` 가 다섯 갈래를 잠근다.

**남은 것은 없다.** 시안(Figma 2671:17922)의 chevron 도 이제 코드와 같다 — #1121 이 그것을
뺐던 것이 의도적 이탈이었고, 그 이탈이 해소됐다.
## #613 — 수신자 홈 설정 진입 (판단 보류)

- 시안(수신자 섹션 `4327:73626`)의 수신자 홈 헤더에는 **톱니만** 있고 프로필 아이콘이 없다.
  코드도 이미 `showProfileIcon = false` 라 1번 지적은 해소된 상태였고, 재발 방지 가드만 붙였다.
- 톱니의 목적지는 확정하지 못했다. 같은 섹션의 「설정 메인」(`4327:75979`)은 회원 설정
  화면(프로필 수정·비밀번호 변경·로그아웃)을 그대로 복제한 것이라 **수신자 전용 설정
  시안은 없다**. 수신자는 X-Auth-Code 기반이라 로그아웃할 세션이 없다.
- 현 배선은 회원 설정 그래프(`Route.Setting`)로 그대로 보낸다. 무엇을 남길지(고객지원만,
  인증 해제, 진입점 제거)는 기획·디자인 확정이 필요해 이번 PR 범위에서 제외했다.
## #793 — MEMORIES 카드 「그날의 기록 다시 읽기」 목적지 (임시 승계)

- **최종 목적지는 여전히 미확정이다.** Figma 카드 노드(`109:15028`)의 이 버튼에는 프로토타입
  연결이 없고, 「정리 Screen Design」 페이지에도 이 버튼의 목적지를 정해 주는 프레임이 없다.
- 그때까지 버튼은 **자기 컨테이너와 같은 곳**(추억 공간)으로 간다. 비워 두면 버튼이 클릭을
  삼켜 카드 안에서 버튼만 죽은 영역이 되기 때문이다 — «미확정이라 비워 둔다» 가 실제로는
  «카드에 구멍을 낸다» 로 나타났다.
- 즉 종전 결정(PR #721 리뷰의 «임의 목적지를 붙이지 않는다»)을 **뒤집은 것이 아니라**,
  새 목적지를 만들지 않는 선에서 구멍만 메운 것이다. 확정이 오면 호출부 인자 하나만 바꾼다.
## #1016 — 일기 본문 이미지 fileKey 변환 (해소, 추측 없음)

한때 «전제가 실측과 어긋난다» 고 적었으나, 서버 복구 후 A/B 실측으로 **이슈 전제가
사실임을 확인했다**. 어긋나 보였던 관측(`diaryId 41541`)은 #958 브랜치에서 나온 것이고,
그 브랜치의 에디터는 이미 `<img src="$fileKey">` 를 넣기 때문에 정상이었다.

2026-08-25 실측 — 같은 일기 생성 API 에 두 형태를 보냈다.

| 보낸 값 | 저장된 값 | CDN |
|---|---|---|
| 전체 URL (`https://cdn/...staging/...png`) | `https://cdn/https://cdn/...permanent/...png` | 403 |
| fileKey (`mindrecords/staging/...png`) | `https://cdn/...permanent/...png` | 200 |

추측으로 진행한 항목이 아니므로 이 문서에서는 근거만 남기고 판단 보류 목록에서 뺀다.

## 소급 기록 5건 (2026-08-28 감사)

디자인 대기 해제(2026-08-23) 뒤 병합된 구현을 감사해, 기록 없이 병합된 재량 결정을 소급해
적는다. 아래 다섯 건 모두 시안·명세에 근거가 없음을 2026-08-28 시안 텍스트 전수 검색으로
재확인했다.

### #506 — 홈 '수신인 지정 미완료' 칩의 목적지

**정한 것**: `SettingRoute.RecipientRegisterRoute`(수신자 등록 화면). **근거가 없는 이유**:
이슈 Note 가 «등록인지 목록인지 기획 확정 전» 이라고 남겼다. **판단 근거**: 칩은
미완료(등록 수신자 0명)일 때만 눌리므로 목록으로 보내면 빈 화면이다 — 0명에서 유효한 다음
행동은 등록뿐이다. **뒤집을 때**: `AppNavigationActions.onRecipientChipClick()` 의
navigate 대상 한 줄.

### #611 — 수신자 목록 전달 조건 미충족 안내 문구

**정한 것**: «아직 열람할 수 없는 상태입니다.» + «발신자가 정한 전달 조건이 충족되면
열람하실 수 있어요.». **근거가 없는 이유**: 시안 수신자 구역에 이 실패 상태의 화면·문구가
없다. **판단 근거**: 403(code 2009)은 재시도가 무의미한 상태라, 원인과 기다릴 대상을
알리는 쪽으로 적었다 — #614 에서 정한 문구 방향과 결이 같다. **뒤집을 때**:
`receiver_afternote_list_not_deliverable_message`·`_description` 두 문자열.

### #619 — 계정 정보 부재의 표현

**정한 것**: 서버가 credentials 를 안 준 계정 섹션은 마스킹 대신 «고인이 계정 정보를
남기지 않았습니다.» / 값 자리 «남기지 않음» 으로 표시한다. **근거가 없는 이유**: 시안에
계정 정보 부재 변형이 없다. **판단 근거**: 가짜 마스킹은 없는 값을 있다고 믿게 한다 —
부재를 타입으로 올리고 화면은 부재를 부재로 말한다. **뒤집을 때**:
`receiver_detail_account_absent`·`receiver_detail_account_value_absent` 와 해당 분기 렌더.

### #739 — 인증번호 발송 중 버튼 문구

**정한 것**: 발송 요청 중 버튼 문구를 «전송 중…» 으로 바꾼다. **근거가 없는 이유**: 시안에
발송 진행 상태 변형이 없다. **판단 근거**: 비활성화만으로는 탭이 접수됐는지 알 수 없다는
이슈 지적을 최소 표현(문구 전환)으로 해소했다. **뒤집을 때**:
`receiver_verify_code_requesting` 문자열 하나.

### #554 — 수신인 검색 placeholder 문구

**정한 것**: 타임레터 수신인 선택·설정 수신자 목록 검색창 placeholder 를 «이름으로
검색하기» 로 적었다. **근거가 없는 이유**: 당시 시안 placeholder 가 리터럴 `Text Field`
였고, 2026-08-15 디자이너 일괄 확정에서도 이 두 검색창은 빠졌다(#794 의 08-16 전수 실측).
**판단 근거**: 리터럴 노출은 QA 결함이라 검색 키(이름)를 안내하는 문구로 채웠다.
**뒤집을 때**: `setting_receiver_search_placeholder`·`timeletter_recipient_search_placeholder`
두 문자열 — 디자이너가 후속 확정하면 그 값으로 바꾼다(#794 참고).

## #648 — 플레이리스트 복수 선택 행의 컨트롤 비주얼

**정한 것**: 곡 선택 행의 라디오 비주얼을 기존 core `AfternoteCircularCheckbox` 로 바꾸고,
행 전체에 `Role.Checkbox` toggleable semantics 를 부여했다.

**근거가 없는 이유**: 시안은 여전히 라디오 비주얼이다. #648 의 「디자인 승인 게이트 해제
(2026-08-28)」는 복수 선택 확정과 checkbox semantics 까지만 정하고, 색·크기·간격은 «최신
플레이리스트 시안과 기존 core 선택 컴포넌트에 맞춰 구현 담당자가 결정» 으로 위임했다.

**판단 근거**: 새 모양을 만들면 미확정 디자인이 굳는다. 이미 있는 core 선택 컴포넌트를
재사용하는 쪽이 되돌리기 쉽고, 복수 선택 실동작과 컨트롤 관용도 일치한다.

**뒤집을 때**: `PlaylistSongItem.SongSelectionCheckbox` 하나다. 시안이 선택 컨트롤을
그리면 그 모양으로 바꾸면 되고, semantics(복수 선택)는 게이트 확정이라 유지된다.

## #490 — 서비스 선택 바텀시트의 검색 규칙과 카탈로그 밖 기존 값

**정한 것 1**: 검색은 trim 한 query 의 대소문자 무시 **부분 문자열** 일치로 하고, 결과는
원본 카탈로그 순서를 유지한다.

**정한 것 2**: 고정 카탈로그에 없는 기존(legacy) custom 서비스 값은 폼에 보존해 그대로
표시하되, 선택 후보 목록에는 올리지 않는다.

**근거가 없는 이유**: 시안은 바텀시트 구성만 보여주고 검색 일치 규칙·정렬 순서를 정의하지
않는다. 기존 custom 값은 「직접 추가하기」 제거(게이트 확정) 뒤에 남는 데이터라 처리 방식이
시안·명세 어디에도 없다.

**판단 근거**: (검색) 짧은 고정 카탈로그라 부분 일치가 관대하고, 재정렬은 시안에 없는 새
규칙을 만드는 셈이라 원본 순서를 지켰다. (기존 값) 지우면 사용자 데이터 파괴고, 후보로
노출하면 제거하기로 한 커스텀 플로우가 사실상 부활한다 — 보존+미노출이 양쪽 결정을 존중한다.

**뒤집을 때**: 검색은 `filterEditorServiceOptions` 함수 하나, 후보 노출은
`EditorFormState.currentServiceOptions` 다. 기존 값 정리(마이그레이션·삭제)가 확정되면
보존 동작을 그 정책으로 바꾼다.

## #1540 — no-op 콜백 디폴트 청소에서 드러난 배선 누락 2건

디폴트를 걷어내자 **컴파일러가 실호출부 2곳을 드러냈다.** 둘 다 사용자에게 버튼이 보이는데
눌러도 아무 일이 없던 자리다. 고친 방향과 근거를 남긴다.

### 1. `TextStyleToolbar` 의 링크·«T» 버튼 (추측 아님)

`WriteTextField` 가 `onLinkClick`·`onTypeClick` 을 넘기지 않아 no-op 이었다. **같은 파일의
`BottomToolbar` 가 같은 두 버튼을 갖고 있어** 배선을 그대로 가져왔다 — 스타일 툴바가 열리면
하단 툴바를 덮으므로 같은 두 affordance 를 유지해야 한다.

| 버튼 | BottomToolbar | 이번에 TextStyleToolbar 에 붙인 것 |
|---|---|---|
| 링크 아이콘(`core_ui_ic_link`) | `sheet = KeyboardSheet.MediaSelect` | 동일 |
| «T» | `showTextStyleToolbar = !showTextStyleToolbar` | 동일(= 열린 툴바를 닫는다) |

### 2. 주간 리포트 HISTORY 의 «더보기» (판단이 들어감)

`WeeklyReportScreen` 이 `DailyQuestionListCard(answer = …)` 만 넘겨, 수정·삭제 메뉴가 뜨는데
둘 다 no-op 이었다.

**배선하지 않고 감췄다.** 주간 리포트는 읽기 전용 요약이고 편집·삭제는 데일리질문 목록의
몫이라고 봤다. 배선하려면 VM 삭제 경로와 편집 내비게이션이 새로 필요해 refactor 범위를
벗어난다. #1540 이 든 처방(「`= {}` 로 죽은 버튼을 그리는 대신 nullable + UI 숨김」)과도 같다.

읽기 전용이 아니라 **편집 가능이 맞다면** 이 판단이 틀린 것이므로, 그때는 `onEdit`·`onDelete`
에 실핸들러를 넘기면 메뉴가 그대로 돌아온다(컴포넌트는 이미 nullable 로 열려 있다).

## #700 — 홈 시안 정본 (해소) + 타임레터 NEXT STEP 문구 (추측 진행)

**정본은 확정됐다 — 추측이 아니다.** 파일에 페이지가 둘뿐이고(`정리 Screen Design`,
`back up`), 이슈가 「구조가 다른 3종」으로 든 `2372:52483` · `3628:18329` · `3628:18722` 는
정본 페이지 트리에 **0건**이다(메타데이터 전수 조회). 정본은 `정리 Screen Design` 안의
섹션 「홈」(`4327:99448`)이고, 회원 홈은 같은 화면의 상태 변형 3종이다.

| 노드 | 상태 |
|---|---|
| `4327:99020` | 수신자지정미완료 · 주간 그리드 사진 있음 |
| `4327:99103` | 수신자지정미완료 · 주간 그리드 빈 상태 |
| `4327:99186` | 수신자지정완료 · 주간 그리드 빈 상태 |

- **기록 카테고리 카드: 세 프레임 모두 없다** (`일기`·`깊은 생각` 텍스트 노드 0건). 앱에
  남아 있던 일기 카드 하나는 백업 시안에서 이어진 잔재였다. 이 PR 이 제거한 근거다.
- **MEMORIES 명칭: `TIME LETTERS MEMORIES`** (세 프레임 렌더 실측).

**타임레터 NEXT STEP 카드 문구 — 추측이 아니라 확정분이었다 (정정).**

한때 이 문서에 「정본 안에서 1:2 로 갈려 디자이너 확인이 남는다」고 적었으나, **#700 에 이미
2026-08-09 디자이너(류하늬) 확인이 기록돼 있었다.** 애프터노트 복제 문구가 전용 문구로
교체된 것이 그 회신의 내용이고, 그 뒤 `blocked: 디자인` 라벨도 제거됐다.

- 본문: **내 자식들의 입학식, 졸업식에 남겨 줄 편지를 작성하셨나요?**
- 버튼: **타임레터 입력하러가기**

`4327:99020` · `4327:99186` 이 아직 애프터노트 문구를 들고 있는 것은 교체가 반영되지 않은
잔재이고, `4327:99103` 이 확정분이다. 세 프레임의 불일치를 «미확정» 으로 읽은 것이 오독이었다.

이 PR 이 그 문구와 목적지(타임레터 탭)를 구현한다. MEMORIES 명칭도 정본 세 프레임이 모두
`TIME LETTERS MEMORIES` 이므로 런타임 리소스를 함께 맞췄다 — 문서에만 적고 두면 확정값과
앱이 어긋난 채 고아가 된다.

## #1067 — 본문 링크 입력의 스킴 처리와 거절 안내

**정한 것 1**: 사용자가 스킴을 적지 않으면 `https` 로 읽는다 (`example.com` → `https://example.com`).

**정한 것 2**: 거절 안내 문구를 «http 또는 https 로 시작하는 주소만 넣을 수 있어요.» 로 적고,
시트를 닫지 않은 채 입력 필드 아래에 보여 준다.

**근거가 없는 이유**: 링크 시트 시안(533:16183)은 제목·완료 버튼·URL 입력 필드만 그리고,
입력 규칙도 실패 상태도 정의하지 않는다. placeholder 도 «URL을 입력하세요.» 뿐이라 스킴을
요구하는지 알 수 없다.

**판단 근거**: (1) 호스트부터 적는 입력이 흔한데 그걸 거부하면 정상 사용을 막는다. 다만
**스킴을 적었다면 그 값을 존중**해서 `javascript:` 앞에 `https://` 를 붙여 정상 주소로
둔갑시키지 않는다. (2) 조용히 무시하면 사용자는 «완료를 눌렀는데 안 들어갔다» 만 보게 된다 —
사유를 알려야 고칠 수 있다. 시트를 닫지 않는 것은 방금 적은 값을 잃지 않게 하기 위해서다.

**정한 것 3**: 스킴 없이 포트만 적은 입력(`example.com:8080`)은 host:port 로 읽는다 —
「`://` 가 없고 콜론 뒤가 전부 숫자」일 때만. `javascript:alert(1)` 은 콜론 뒤가 숫자가 아니라
그대로 스킴 선언으로 남는다.

그 판정이 함께 통과시키는 것 셋을 **알고 남긴다** (#1067 리뷰).

| 입력 | 결과 | 판단 |
|---|---|---|
| `javascript:1234` | `https://javascript:1234` | 스킴이 https 로 강제돼 **위험하지 않다** |
| `tel:0212345678` | `https://tel:0212345678` | 전화번호를 적으면 열리지 않는 링크가 조용히 생긴다 |
| `https://example.com:99999` | 그대로 | 포트 범위(0–65535)를 보지 않는다 |

세 경우 모두 **본문에 실려도 남에게 해가 되지 않는다** — 이 함수가 막으려는 것은 스킴 주입과
호스트 위장이고 그 둘은 닫혔다. 「열리지 않는 링크」와 「범위 밖 포트」는 사용자가 자기 입력으로
만든 결과라, 지금 더 조이면 정상 입력까지 막을 위험이 크다고 보아 두었다. 조이려면
`looksLikeHostPort` 에 «콜론 앞이 유효 호스트» 와 포트 범위를 더하면 된다.

**뒤집을 때**: 스킴 기본값은 `toBodyLinkHrefOrNull` 의 `DEFAULT_SCHEME` 상수 하나,
문구는 `mindrecord_link_sheet_invalid` 하나, 포트 판정은 `looksLikeHostPort` 하나다.

> 스킴 제한(`http`/`https` 만) 자체는 추측이 아니다 — 본문은 수신자가 열람하는 저장 값이라
> `javascript:` 를 넣을 수 있으면 남에게 실린다. 되돌릴 대상이 아니다.

## #1487 — MEMORY SPACE 헤더 타이포

- **제목까지 함께 고친 것**: 이슈 본문이 제목(11/16.5 vs 토큰 13/21)을 「범위 밖, 판단은 담당자 몫」으로 넘겼다. 같은 컴포저블의 인접 3줄이고 시안 노드(`4327:67831`)로 값이 확정돼 한 PR 로 묶었다. 나누면 같은 자리를 두 번 고치고 스크린샷 골든도 두 번 재생성해야 한다.
- **색·자간은 건드리지 않았다(추측 아님, 범위 판단)**: 같은 노드 조회에서 두 가지가 더 어긋난다.
  - 부제 색 — 시안 `#757575`(= `gray6`), 코드 `gray5`. **이 줄의 전제는 그 뒤 뒤집혔다** — `#1798`(PR #1799)이
    라이트의 `gray5` 만 `#9E9E9E` → `#6E6E6E` 로 내려 본문 대비를 2.57:1 에서 4.89:1 로 올렸다. 그래서 지금은
    **코드가 시안보다 어둡다.** 다크의 `gray5` 는 `#9E9E9E` 그대로다(그쪽은 `#212121` 위 6.01:1 로 이미 통과).
  - 자간 — 시안 부제 `+0.167px`(≈ +0.019em) · 제목 `+0.6145px`(≈ +0.056em), 코드 토큰 `-0.006em` (부호가 반대)

  선례(#1403 → PR #1442)가 `fontSize` + `lineHeight` 만 다뤘고, 색·자간은 판정 축이 달라(대비·가독성 / 토큰 전반) 별도 이슈로 뗐다. 이 PR 에서 조용히 바꾸면 리뷰가 세 축을 한꺼번에 봐야 한다.

  > **색 축에 남은 질문은 「시안에 맞출 것인가」가 아니다.** 시안 값 `#757575` 자체가 `#FAFAFA` 위에서
  > **4.41:1 로 본문 최소 4.5:1 에 미달한다**(같은 식으로 잰 값 — `#9E9E9E` 2.57:1 · `#6E6E6E` 4.89:1).
  > 즉 시안을 그대로 따르는 선택지가 애초에 없다. 남은 것은 「대비를 위해 시안보다 어둡게 둔 것을
  > 시안 쪽에서 고칠 것인가」이고, 그건 코드가 아니라 디자이너에게 갈 질문이다.

## #1471 — 주간 요약 「RECORDED MOMENT」 이미지의 접근성 라벨

**리소스화가 아니라 `null`(장식)로 판정했다.** 이슈가 「값도 함께 볼 것 — 기계적으로 영어 문구를
그대로 리소스에 넣지 말 것」이라고 판단을 넘긴 자리다.

근거: 이 이미지 **위에 «RECORDED MOMENT» 텍스트가 겹쳐 그려진다.** 이미지에 라벨을 주면
스크린리더가 같은 뜻을 두 번 읽는다. 누를 대상도 이미지가 아니라 감싼 `Surface(onClick=)` 이고,
그쪽이 클릭 semantics 와 (겹친 텍스트로) 라벨을 갖는다.

**한국어 문구를 새로 만들지 않은 이유**: 그러면 화면에 보이는 라벨(영어)과 낭독되는 라벨(한국어)이
갈린다. 시안이 `THIS WEEK`·`WEEKLY SUMMARY` 처럼 영어 mono 라벨을 쓰는 화면이라, 그 문구를
한국어로 바꿀지는 이 이슈가 아니라 시안 판정 몫이다.

## #1580 — 「기록된 순간들」 행간

**이슈 제목의 「시안 160%」는 틀렸다.** 160% 는 원 토큰 `footnoteCaption`(10/16)의 배율이고,
시안 노드([`4327:99094`](https://www.figma.com/design/UP9ZR186jHvRBicjA2SOea/?node-id=4327-99094))
실측은 **11 / 16.5px = 150%** 다. 그 값으로 `lineHeight = 16.5.sp` 를 명시했다.

`letterSpacing` 은 그대로 뒀다 — 시안 `0.0645px`(≈0.0059em) 대 코드 `0.005em` 로 0.01px 차이라
육안·렌더 차이가 없고, 이미 의도적으로 명시된 값이다.

## #1523 — `UserProfileRepository` 과도기 별칭

**별칭을 걷지 못했다.** 이슈는 「`feature:home` 소비자 2곳만」을 전제했는데, 착수 시점에는
**afternote 쪽 소비자가 더 있다**(`AfternoteDetailViewModel` · 테스트 fixture 2곳, @1hyok 담당).
그쪽이 옮기기 전에 `typealias` 를 지우면 남의 모듈이 깨진다.

이 PR 은 home 몫(+ 그 생성자 파라미터명 변경이 강제한 `HomeWeeklyCountAndroidTest` 1곳)만 옮기고
별칭은 남긴다. 걷는 것은 afternote 이관 뒤의 후속이다.

## #1179 — 「48dp 미만 11곳」이 실측에서 0건이었다

이슈가 든 11곳은 **후보**였고, 본문이 「스캐너로 렌더 후 노드 bounds 를 재서 확정한 뒤 착수한다」
고 못 박아 뒀다. #1167 의 스캐너로 재 보니 **크기 미달은 0건**이다.

    더보기 메뉴   touch=48.0dp×48.0dp   layout=20.0dp×20.0dp
    임시저장      touch=48.0dp×48.0dp   layout= 2.0dp×17.5dp
    링크·T       touch=48.0dp×48.0dp   layout=24.0dp×24.0dp

`layout` 은 작아도 **시스템 최소 터치 타깃 확장**이 붙어 `touch`(실제 hit-test 영역)는 전부 48dp
이상이다. 스캐너가 크기 판정에 쓰는 값도 `touch` 다.

**그래서 `minimumInteractiveComponentSize` 를 붙이지 않았다.** 붙였다면 이미 48dp 인 hit 영역은
그대로인 채 **레이아웃 footprint 만 48dp 로 밀려** 시안과 어긋났을 것이다 — 고칠 것이 없는 자리에
회귀를 만드는 셈이다.

대신 같은 스캐너가 드러낸 **다른 계약 위반**을 고쳤다(이름 3·Role 16). 중첩 클릭 3건은 공용
스캐너의 예외를 넓힐지 카드를 재구성할지가 이 모듈 혼자 정할 문제가 아니라 #1669 로 뗐다.

### 정렬과 서식의 semantics 를 다르게 준 근거

종전에는 둘 다 맨 `clickable` 이었다.

- 정렬 3종 — 셋 중 **하나**를 고른다 → `selectable` + `Role.RadioButton` + `selectableGroup`
- 서식 B·I·U·S — 서로 **독립**인 켜기/끄기 → `toggleable` + `Role.Checkbox`
- 스타일 4종(제목·머릿말·부머릿말·본문) — 넷 중 하나 → 정렬과 같은 처방

한 헬퍼(`PillIconSlot`)를 정렬과 서식이 공유하고 있어 `singleChoice` 로 갈랐다. 같은 모양이라도
스크린리더가 읽는 말이 달라야 한다.

## #1179 — 스타일 패널 「T」 버튼의 이름

**「글자 종류」 로 붙였다 — 추측이다.** 이 버튼(`onTypeClick`)은 **어디에도 연결돼 있지 않다**:
기본값이 `{}` 이고 유일한 호출부(`WriteTextField`)가 값을 넘기지 않는다. 그래서 코드만으로는
무엇을 여는 버튼인지 알 수 없다.

Figma 노드 `552:19970` 로 확인하려 했으나 데스크톱 앱에 해당 문서가 열려 있지 않아 실패했다.
바로 아래 「글자 종류」 행(제목·헤더·소제목·본문)이 있어 그 행이 하는 일로 이름을 잡았다.

**그냥 두는 선택지는 없었다** — 클릭 semantics 는 실려 있어서, 이름을 안 주면 스크린리더가
「T, 버튼」 으로 읽는다. 시안이 확정되면 이 문자열
(`mindrecord_toolbar_text_type_cd`)만 고치면 된다.

## #1179 — 후보 전량을 스캐너로 덮었다

이슈가 든 후보를 **하나도 빼지 않고** 스캐너 테스트로 고정했다. `DiaryWriteScreen` 은 화면
전체가 `hiltViewModel()` 을 물어 Robolectric 에서 그대로 못 띄우지만, `DiaryWriteScreenContent`
가 상태만 받는 seam 으로 열려 있어 그쪽을 렌더한다.

**넓히자마자 그 화면에서만 위반 3종이 나왔다.** 두 툴바만 보던 동안에는 드러나지 않던 것들이다.

| 위반 | 자리 | 처방 |
|---|---|---|
| 이름 없음 | 본문 편집기 (288×461dp) | 보이는 안내 문구와 같은 문자열을 `contentDescription` 으로 |
| Role 없음 | 수신자 행 | `Role.Button` |
| Role·선택 상태 없음 | 기분 칩 3종 | `selectableGroup` 안의 `Role.RadioButton`, 이름은 이모지 대신 낱말 |

본문 편집기는 `DailyQuestionWriteScreen` 도 같은 컴포넌트를 쓰므로 두 화면이 함께 고쳐졌다.
보이는 안내 문구는 `clearAndSetSemantics {}` 로 접근성 트리에서 뺐다 — 같은 문장을 두 번 읽지
않게 하려는 것이고, material3 의 `TextField` 가 placeholder 를 다루는 방식과 같다.

## #1179 — material3 가 소유한 타깃 2건

필터 시트 판정에서 둘을 뺐다.
- **시트 바깥 scrim**(`Close sheet`) — 이름도 역할도 라이브러리 소관이라 이 저장소에서 못 고친다.
- **드래그 손잡이** — 클릭 액션을 material3 가 얹는 슬롯이다. 이름은 우리가 채웠고(비어 있었다),
  Role 은 주지 않았다 — 버튼도 선택도 아니라 붙이면 오히려 틀리게 읽힌다.

## #1712 — 일기 탭 문구 2건은 시안에서 **의도적으로 이탈**했다

**정정.** 이 절은 종전에 「시안 조회에 실패해 추측했다」고 적었는데 사실과 다르다. 리뷰에서
정본이 열려 실측됐고(2026-09-03 재확인), **시안은 일기 탭에도 데일리질문과 같은 문구를 쓴다.**

| 자리 | 정본 시안 | 이 저장소 |
|---|---|---|
| 캘린더 카운트 | `18개의 답변 완료` | `N개의 기록 완료` |
| 목록 섹션 헤더 | `DAILY ANSWER` | `DAILY DIARY` |

정본 페이지 일기_캘린더 형 [`4327:77693`](https://www.figma.com/design/UP9ZR186jHvRBicjA2SOea/?node-id=4327-77693)
(감정 표시 없는 판 `4327-77678` 도 같다).

**그럼에도 이탈을 유지한다.** 일기는 질문에 답한 것이 아니라 그날을 적은 것이고, 「답변」은 이 앱이
데일리질문에만 쓰는 말이다(`오늘의 질문`·`답변 완료`). 같은 화면의 데일리질문 탭이 바로 옆에서
같은 문구를 쓰고 있어, 두 탭이 같은 말을 하면 무엇이 다른 탭인지 사라진다. 데일리질문 프레임의
문구가 일기 프레임으로 복사되며 함께 넘어온 것으로 본다.

**확정은 아니다 — 디자인 회신이 필요하다.** 회신이 「시안대로」면 문자열 두 개
(`mindrecord_calendar_recorded_count`·`mindrecord_diary_list_section_header`)만 되돌리면 된다.
헤더를 아예 없애는 판정이 나와도 그 자리만 지우면 된다. 리터럴을 리소스로 옮기는 것 자체는
어느 판정에서도 남는다.

## #1548 — MEMORY SPACE 자간을 토큰이 아니라 이 자리에서 덮은 이유

이슈가 「토큰을 손댈지 이 화면만 `copy` 할지」를 판정으로 넘겼다. **이 자리에서만 덮었다.**

`typography.inter` 는 afternote·home·mindrecord·timeletter **4개 모듈 12곳**이 쓰고 담당도 갈린다.
토큰의 `letterSpacing`(`-0.006em`)을 시안 값(양수)으로 뒤집으면 그 12곳이 전부 따라 움직이므로,
소비자 전수 시안 대조가 선행돼야 하는 별건이다. 이 PR 은 시안이 확정된 두 문구만 맞춘다.

em 환산은 시안 px 를 fontSize 로 나눈 값이다 — 제목 `0.6145/11 = 0.055864` → `0.0559.em`,
부제 `0.167/9 = 0.018556` → `0.0186.em`. 넷째 자리에서 끊어 시안과 0.0004px 차이다.

## #1562 — 제출 성공 매핑을 계측이 아니라 소스로 지킨 이유

이슈가 「비용이 값을 넘으면 KDoc 명시로 닫아도 된다」고 열어 둔 자리다. **KDoc 과 konsist 가드
둘 다** 했다.

계측으로 태우려면 두 화면 모두 `hiltViewModel()` 과 서버 제출을 지나야 해 #1466 의 뒤로가기 층보다
비용이 크다. 대신 `MindRecordNavGraphWiringKonsistTest` 가 **소스의 매핑 자체**를 본다 — 계측은
「그 명령이 무엇을 하나」를, konsist 는 「어느 명령에 붙었나」를 본다.

한계도 적어 뒀다: 텍스트 검사라 `{ actions.popBack() }` 같은 다른 표기는 헛짚는다. 매핑 7줄이
같은 모양이어야 한 눈에 대조되므로, 그때는 가드가 아니라 표기를 맞추는 편이 낫다.
## #793 — 「그날의 기록 다시 읽기」 목적지 확정

이슈의 디자인 승인 게이트가 2026-08-28 에 해제돼 **FE 가 목적지를 정했다.** 후보 셋 중 (a)
**그 기록의 상세**를 골랐다.

**근거**: 버튼 문구가 「**그날의** 기록 다시 읽기」다 — 카드가 보여 주는 그 한 건을 가리키는 말이라
목록 성격의 목적지(추억 공간·마인드레코드 목록)와 어울리지 않는다. 카드·섹션 전체는 종전대로
추억 공간으로 두어 두 목적지가 갈린다.

**이슈 작성 시점과 달라진 것**: 그때는 기록 상세 화면 자체가 없었다. `RecordDetailRoute` 가 #759
로 들어와 (a) 가 비로소 구현 가능해졌다.

**0건이면 상세로 보내지 않는다** — 열 기록이 없는데 상세를 열면 빈 화면이 된다. 그때만 카드와
같은 곳(추억 공간)으로 간다.

카드가 싣는 것은 현재 **가장 최근 데일리질문 답변** 한 건이라 `isDiary = false` 로 고정했다.
카드가 나중에 일기까지 포함하게 되면 종류를 함께 넘겨야 한다 — 그 자리에 주석을 남겼다.

## #1404 — 정렬 아이콘을 어느 쪽 알파 계약으로 모을 것인가

**처음 판단을 뒤집었다.** 승격본에서 `android:strokeAlpha="0.6"` 을 걷어냈다가, 리뷰 지적을 받고
되살렸다.

걷어낸 논리는 「드로어블에 알파를 박아 두면 호출부의 `tint` 위에 한 번 더 곱해진다」였고, 그 자체로는
맞다. 놓친 것은 **비교 대상이 그대로였다는 점**이다 — timeletter 는 여전히 알파가 있는 자기 사본을
보고 있었으므로, 알파를 한쪽에서만 걷은 결과 같은 2.4 스트로크가 두 화면에서 다른 농도로 찍혔다.
갈라진 렌더를 모으는 것이 이 이슈인데 그 반대로 간 셈이다.

**지금 판**: 승격본을 develop 의 timeletter 원본과 **바이트 동일**로 되돌리고(3종 diff 확인),
timeletter 호출부를 승격본으로 옮긴 뒤 모듈 사본을 걷었다. 두 모듈이 같은 파일 하나를 보므로
어긋날 자리가 없고, timeletter 의 렌더는 같은 벡터라 변하지 않는다.

**timeletter 를 건드린 이유**: 남의 담당 모듈이지만, 「양쪽을 같은 알파 계약으로 맞추라」는 리뷰
요구를 사본을 남긴 채로는 닫을 수 없다. 변경은 리소스 참조 3줄 + 사본 삭제뿐이고 렌더는 그대로다.

**알파를 호출부로 올리는 것은 이 이슈가 아니다.** 그건 두 모듈의 tint 계약을 함께 바꾸는 일이라
사본 수렴(#635)이 끝난 뒤에 한 벌만 고치는 편이 안전하다.

## #1798 — 라이트 본문 대비를 토큰에서만 올린 범위 판단

**라이트만 고치고 다크는 `#9E9E9E` 를 그대로 뒀다.** 팔레트 반전에서 `gray5` 만 자기 자신으로
매핑되는데 배경이 뒤집히므로 같은 값의 대비가 갈린다 — 라이트 `#FAFAFA` 위 2.57:1(미달),
다크 `#212121` 위 6.01:1(통과). 다크까지 함께 내리면 통과하던 쪽을 3.16:1 로 떨어뜨린다.

**시안 값(`#757575`)으로 맞추지 않았다.** 그 값도 `#FAFAFA` 위에서 4.41:1 이라 본문 최소 4.5:1 에
미달한다. 시안을 따르는 선택지가 없어서 기준을 넘는 최근접 값 `#6E6E6E`(4.89:1)를 골랐다.
시안과 코드가 갈리는 것은 의도된 차이다 — `#1487` 절의 「코드가 시안보다 밝다」는 이 PR 로 부호가 뒤집혔다.

**토큰을 지나지 않는 아이콘은 범위 밖으로 뒀다(#1861).** 색을 드로어블에 박고 `Image` 로 그려
tint 를 안 받는 자리가 3개 모듈 6곳 남아 있고, 그쪽은 계속 2.57:1 이다. `Icon(tint = ...)` 으로
그리는 자리(`ContentListItem.kt:79`)는 토큰이 색을 덮으므로 이 PR 로 함께 고쳐진다.
모듈이 afternote·setting·timeletter 로 갈려 담당자가 둘이라 한 PR 로 묶지 않았다.
